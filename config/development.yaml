server:
  protocol: http
  host: 127.0.0.1
  port: 7150

logger:
  level: trace # off, warn, trace, error, info, debug
  format: pretty # json, pretty, compact, full
  ## Crates to log i.e *name of your crate*, sqlx, axum, etc
  crates:
    - "betterauth"
    - "axum"
    - "sqlx"
    - "tower-http"

database:
  uri: postgresql://postgres:postgres@localhost:5432/postgres
  name: postgres
  host: localhost
  port: 5432
  password: postgres
  user: postgres
  protocol: postgresql
  # Migrate the database on application startup
  auto_migrate: true
  ## Dangerous operations that will either clear data from all tables
  ##  or recreate the entire database, both resulting in data losses
  truncate: false
  recreate: false

auth:
  access:
    private_key: security/keys/dev/access_key.pem
    public_key: security/keys/dev/access_key_pub.pem
    exp: 900 # 15 Minutes
  refresh:
    private_key: security/keys/dev/refresh_key.pem
    public_key: security/keys/dev/refresh_key_pub.pem
    exp: 604800 # 7 Days